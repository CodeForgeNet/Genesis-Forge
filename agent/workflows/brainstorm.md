---
description: "You MUST use this before any creative work - creating features, building components, adding functionality, or modifying behavior. Explores requirements and design before implementation."
disable-model-invocation: true
---

Invoke the `brainstorming` skill from the `agent/skills/` directory and follow it exactly as presented.

If a `brainstorming` skill is not found in the registry, run:
```
python3 agent/scripts/skill_search.py "brainstorming requirements exploration"
```
and use the top-ranked result instead.
